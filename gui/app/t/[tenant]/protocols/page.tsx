/** 프로토콜 — 빈 라우트. 파일트리 + md 에디터는 4c687667이 채운다. */
export default function Protocols() {
  return (
    <div className="space-y-4">
      <h1 className="text-lg font-semibold">프로토콜</h1>
      <p className="text-sm text-muted-foreground">프로토콜 파일트리는 다음 티켓에서 붙는다.</p>
    </div>
  );
}
