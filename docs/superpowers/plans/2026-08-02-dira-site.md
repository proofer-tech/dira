# dira.proofer.tech 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/site`에 VitePress 사이트 한 벌을 세워 랜딩·매뉴얼 18장·이용약관·개인정보처리방침을 `dira.proofer.tech` 한 도메인으로 낸다.

**Architecture:** 기존 `docs/index.html`(543줄)의 디자인 토큰과 마크업을 VitePress 커스텀 테마로 이식한다. 랜딩은 `Landing.vue` 한 컴포넌트, 매뉴얼·약관은 마크다운, 스타일은 `custom.css` 한 벌. 버전 문자열은 본문에 적지 않고 `apps/desktop/package.json`에서 빌드 때 읽는다. 배포는 Vercel 프로젝트 하나(root `apps/site`).

**Tech Stack:** VitePress 1.x, Vue 3(SFC 한 개), pnpm 11, Node 25, Vercel.

**스펙:** `docs/superpowers/specs/2026-08-02-dira-landing-and-docs-design.md`

## Global Constraints

- **한국어 전용.** 영문 페이지를 만들지 않는다.
- **조직명은 `proofer-tech`다** — 하이픈이 있다. `proofertech`는 존재하지 않는 계정이다.
- **버전 문자열을 본문에 적지 않는다.** `apps/site/version.ts`가 유일한 출처다. 실측 2026-08-02: 이 계획을 쓰는 몇 시간 사이 `0.1.4` → `0.1.5`로 올랐다(`release.yml`이 master 커밋마다 bump한다).
- **쿠키를 심지 않는다.** `gtag.js`·외부 폰트·외부 스크립트 금지. 방문 통계는 Vercel Analytics(쿠키리스)뿐이다.
- **`apps/site`는 독립 pnpm 프로젝트다.** 자기 `pnpm-workspace.yaml`과 `pnpm-lock.yaml`을 가진다. 레포 루트에 `package.json`도 워크스페이스도 없다(`ci.yml` 주석).
- **테스트는 `node --test "*.test.ts"`다.** `apps/desktop`과 같은 벌이다. 프레임워크를 새로 들이지 않는다.
- **레포 루트 `docs/`에 VitePress를 붙이지 않는다.** `docs/DESIGN.md`는 8천 줄 내부 계약서다.
- Node 25 · pnpm 11 (`ci.yml`의 `setup-node`·`action-setup`과 같은 값).

---

### Task 1: apps/site 골격과 버전 잠금

VitePress가 도는 최소 사이트를 세우고, 버전 하드코딩을 테스트로 막는다.

**Files:**
- Create: `apps/site/package.json`
- Create: `apps/site/pnpm-workspace.yaml`
- Create: `apps/site/.gitignore`
- Create: `apps/site/version.ts`
- Create: `apps/site/version.test.ts`
- Create: `apps/site/.vitepress/config.ts`
- Create: `apps/site/index.md`

**Interfaces:**
- Produces: `version.ts` → `export const diraVersion: string`. Task 2·3의 `Landing.vue`가 `useData().theme.value.diraVersion`으로 읽는다.

- [ ] **Step 1: 버전 잠금 테스트를 먼저 쓴다**

`apps/site/version.test.ts`:

```ts
/** 랜딩의 버전 표기가 실제 릴리스와 갈리지 않게 잠근다.
 *  손으로 적으면 `release.yml`이 master마다 bump하는 동안 영구히 어긋난다
 *  (실측 2026-08-02: 반나절에 0.1.4 → 0.1.5). */
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { diraVersion } from "./version.ts";

test("diraVersion이 apps/desktop/package.json의 version과 같다", () => {
  const pkg = JSON.parse(
    readFileSync(new URL("../desktop/package.json", import.meta.url), "utf8"),
  );
  assert.equal(diraVersion, pkg.version);
  assert.match(diraVersion, /^\d+\.\d+\.\d+$/);
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/site && node --test "*.test.ts"
```

기대: `Cannot find module './version.ts'`로 실패.

- [ ] **Step 3: `version.ts`를 만든다**

```ts
/** 랜딩·푸터의 버전 표기는 전부 여기서 온다. **본문에 숫자를 적지 않는다** —
 *  `release.yml`이 master 커밋마다 bump하므로 손으로 적는 한 반드시 어긋난다. */
import { readFileSync } from "node:fs";

export const diraVersion: string = JSON.parse(
  readFileSync(new URL("../desktop/package.json", import.meta.url), "utf8"),
).version;
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd apps/site && node --test "*.test.ts"
```

기대: 1 pass.

- [ ] **Step 5: pnpm 프로젝트 파일 셋을 만든다**

`apps/site/package.json`:

```json
{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "test": "node --test \"*.test.ts\""
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.13"
  }
}
```

`apps/site/pnpm-workspace.yaml` — 빈 워크스페이스 선언 하나. 이 파일이 있어야 pnpm이 상위를 훑지 않는다:

```yaml
# apps/teams·apps/desktop과 같은 벌이다 — 각자 독립 프로젝트다(`ci.yml` 주석).
packages: []
```

`apps/site/.gitignore`:

```
node_modules
.vitepress/dist
.vitepress/cache
```

- [ ] **Step 6: VitePress 설정과 임시 홈을 만든다**

`apps/site/.vitepress/config.ts`:

```ts
import { defineConfig } from "vitepress";
import { diraVersion } from "../version.ts";

export default defineConfig({
  lang: "ko-KR",
  title: "dira",
  description:
    "티켓을 큐에 넣으면 cron에 물린 워커가 claude 세션에 넘긴다. 파일시스템이 곧 큐인 티켓 디스패처.",
  cleanUrls: true,
  // 죽은 내부 링크는 빌드를 깨뜨린다. 매뉴얼 18장의 링크 검사를 이것으로 대신한다.
  ignoreDeadLinks: false,
  head: [["link", { rel: "icon", href: "/icon.svg", type: "image/svg+xml" }]],
  themeConfig: {
    diraVersion,
  },
});
```

`apps/site/index.md`:

```markdown
# dira

골격 확인용 임시 홈이다. Task 2에서 `Landing.vue`가 대체한다.
```

- [ ] **Step 7: 설치하고 빌드가 도는지 본다**

```bash
cd apps/site && pnpm install && pnpm build
```

기대: `.vitepress/dist/index.html` 생성. 에러 없음.

- [ ] **Step 8: 커밋**

```bash
git add apps/site
git commit -m "feat(apps/site): VitePress 골격 — 버전은 desktop/package.json에서 읽는다

랜딩 본문에 버전을 적지 않는다. release.yml이 master 커밋마다 bump하므로
손으로 적는 한 어긋난다 — 실측 2026-08-02에 반나절 만에 0.1.4에서 0.1.5로 올랐고
기존 docs/index.html은 v0.1.2에 멈춰 있었다. version.test.ts가 그 어긋남을 깨뜨린다."
```

---

### Task 2: 디자인 토큰·스크린샷 이식과 랜딩 셸

기존 페이지의 스타일과 헤더·히어로·푸터를 옮긴다. 나머지 섹션은 Task 3·4가 채운다.

**Files:**
- Create: `apps/site/.vitepress/theme/index.ts`
- Create: `apps/site/.vitepress/theme/custom.css`
- Create: `apps/site/.vitepress/theme/Landing.vue`
- Modify: `apps/site/index.md`
- Move: `docs/shots/*` → `apps/site/public/shots/`
- Move: `docs/icon.svg` → `apps/site/public/icon.svg`

**Interfaces:**
- Consumes: `themeConfig.diraVersion` (Task 1)
- Produces: `Landing.vue`의 `<template>` 안에 `<!-- SECTION: n -->` 주석 자리표. Task 3·4가 그 자리에 섹션을 끼운다.

- [ ] **Step 1: 정적 자산을 옮긴다**

```bash
mkdir -p apps/site/public
git mv docs/shots apps/site/public/shots
git mv docs/icon.svg apps/site/public/icon.svg
```

- [ ] **Step 2: `custom.css`를 만든다**

`docs/index.html`의 `<style>` 안(12–199줄)을 그대로 옮긴다. `<style>`·`</style>` 태그만 뺀다. 그 위에 VitePress 기본 테마와 겹치는 것을 끊는 블록을 얹는다:

```css
/* 랜딩은 VitePress 기본 레이아웃 밖에서 자기 폭을 쓴다. */
.dira-landing { --vp-c-bg: var(--bg); }
.dira-landing .wrap { max-width: var(--maxw); margin: 0 auto; padding: 0 24px; }

/* 아래는 docs/index.html:12-199에서 그대로 옮긴 것이다. 값은 손대지 않는다 —
   oklch 상태 색 넷(--active/--blocked/--done/--stale)은 앱 화면의 레인 색과 같은 값이다. */
```

**`word-break: keep-all`을 반드시 살린다.** 한국어 줄바꿈이 이것 없이는 어절 중간에서 끊긴다.

- [ ] **Step 3: 테마를 등록한다**

`apps/site/.vitepress/theme/index.ts`:

```ts
import DefaultTheme from "vitepress/theme";
import Landing from "./Landing.vue";
import "./custom.css";

export default {
  extends: DefaultTheme,
  enhanceApp({ app }) {
    app.component("Landing", Landing);
  },
};
```

- [ ] **Step 4: `Landing.vue` 셸을 만든다**

`docs/index.html`의 다음 덩이를 옮긴다:
- 공지 바 (203–208줄) — **`v0.1.2` 하드코딩을 `{{ version }}`으로 바꾼다**
- 헤더 (210–221줄) — `href="./"`를 `to="/"`로, `앱 받기` 버튼 유지
- 히어로 (225–236줄) — **두 번째 CTA를 `#engine` 앵커에서 `/docs/install`로 바꾼다.** `cta-note`의 `v0.1.2`도 `{{ version }}`
- 클로징 (485–497줄), 푸터 (499–540줄)

푸터 `문서` 칼럼을 이렇게 바꾼다 — 이제 매뉴얼이 있다:

```html
<div class="fcol">
  <h4>문서</h4>
  <ul>
    <li><a href="/docs/">매뉴얼</a></li>
    <li><a href="/docs/install">설치 가이드</a></li>
    <li><a href="https://github.com/proofer-tech/dira#readme">README</a></li>
    <li><a href="https://github.com/proofer-tech/dira/tree/master/templates">템플릿</a></li>
  </ul>
</div>
```

푸터 `.fbot`에 법적 링크와 사업자 정보를 넣는다:

```html
<div class="fbot">
  <span>© 2026 프루퍼 주식회사. MIT.</span>
  <a href="/terms">이용약관</a>
  <a href="/privacy">개인정보처리방침</a>
  <span class="sep">bash + python3 표준 라이브러리</span>
</div>
```

스크립트 블록:

```vue
<script setup>
import { useData } from "vitepress";
const { theme } = useData();
const version = theme.value.diraVersion;
</script>
```

나머지 섹션 자리에는 주석만 둔다:

```html
<!-- SECTION 2: 30초 설명 — Task 3 -->
<!-- SECTION 3: 4기둥 — Task 4 -->
<!-- SECTION 4: 실제 화면 — Task 4 -->
<!-- SECTION 5: 60초 설치 — Task 3 -->
<!-- SECTION 6: 여기 없는 것 — Task 4 -->
<!-- SECTION 7: 플랜 — Task 3 -->
```

- [ ] **Step 5: `index.md`를 랜딩으로 바꾼다**

```markdown
---
layout: page
pageClass: dira-landing
---

<Landing />
```

- [ ] **Step 6: 매뉴얼이 생길 때까지 죽은 링크를 유예한다**

푸터와 히어로가 `/docs/…`를 가리키는데 그 파일은 Task 5–8이 만든다. 그때까지 `config.ts`에서 이 한 줄만 바꾼다:

```ts
// 유예다. 매뉴얼 18장이 다 들어오는 Task 8에서 `false`로 되돌린다.
ignoreDeadLinks: [/^\/docs\//],
```

**`/docs/` 밖의 죽은 링크는 계속 빌드를 깨뜨린다** — 유예 범위를 넓히지 않는다.

- [ ] **Step 7: 빌드하고 눈으로 본다**

```bash
cd apps/site && pnpm build && pnpm preview
```

확인할 것 셋: 헤더·히어로·푸터가 뜬다 / 버전이 `0.1.5`(현재 `package.json` 값)로 보인다 / 보드 스크린샷이 깨지지 않는다.

- [ ] **Step 8: 커밋**

```bash
git add apps/site docs
git commit -m "feat(apps/site): 토큰·스크린샷 이식과 랜딩 셸

docs/index.html:12-199의 스타일을 값 그대로 옮겼다 — oklch 상태 색 넷은 앱 화면의
레인 색과 같은 값이라 손대지 않는다. word-break: keep-all도 그대로다.

히어로의 두 번째 CTA를 #engine 앵커에서 /docs/install로 바꾼다. 요구가 설치
가이드였고, 이제 갈 데가 생겼다."
```

---

### Task 3: 랜딩 신설 3섹션 — 30초 설명 · 60초 설치 · 플랜

처음 오는 사람이 용어를 만나는 자리다. 기존 페이지에 없던 층이다.

**Files:**
- Modify: `apps/site/.vitepress/theme/Landing.vue` (SECTION 2·5·7 자리)
- Modify: `apps/site/.vitepress/theme/custom.css` (`.steps`, `.plan` 추가)

- [ ] **Step 1: SECTION 2 — 30초 설명을 넣는다**

`.pillars` 앞에 온다. 4기둥이 티켓·워커·큐라는 단어로 시작하므로 그 정의가 먼저 와야 한다.

```html
<section class="wrap">
  <p class="eyebrow">30초</p>
  <h2>파일 하나 쓰면, cron이 물어 간다</h2>
  <ol class="steps">
    <li>
      <b>① 티켓을 쓴다</b>
      <p>마크다운 파일 하나다. 무엇을 원하는지와 무엇이 되면 끝인지를 적는다.
      큐는 그 파일들이 담긴 <b>디렉터리 하나</b>가 전부다.</p>
    </li>
    <li>
      <b>② 워커가 문다</b>
      <p><b>워커</b>는 cron에 걸린 셸 스크립트 한 줄이다. 1분에 한 번 깨어나 열린 티켓
      하나를 골라 <code>claude -p</code> 세션에 넘기고, 그 세션이 끝날 때까지 기다린다.</p>
    </li>
    <li>
      <b>③ 도는 걸 본다</b>
      <p>세션이 무엇을 읽고 무엇을 고치는지 화면에서 따라간다.
      <b>끝난 뒤 로그를 뒤지는 게 아니라 도는 동안 말을 건다.</b></p>
    </li>
  </ol>
</section>
```

CSS:

```css
.steps { list-style: none; margin: 32px 0 0; padding: 0; display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
.steps li { border: 1px solid var(--border); border-radius: 12px; padding: 22px; background: var(--panel); }
.steps b { display: block; margin-bottom: 8px; font-size: 15px; letter-spacing: -0.02em; }
.steps p { margin: 0; font-size: 14px; color: var(--muted-fg); line-height: 1.65; }
@media (max-width: 900px) { .steps { grid-template-columns: 1fr; } }
```

- [ ] **Step 2: SECTION 5 — 60초 설치를 넣는다**

랜딩에서 바로 깔 수 있어야 한다. `README.md §설치`·`§빠른 시작`이 원본이다.

```html
<section class="wrap">
  <p class="eyebrow">60초</p>
  <h2>설치는 clone이 끝이다</h2>
  <p class="body" style="max-width:44em">
    엔진은 <b>bash와 python3 표준 라이브러리</b> 밖으로 나가지 않는다.
    빌드도, 패키지 매니저도, 설정 파일도 없다.
  </p>
  <div class="two" style="margin-top:32px">
    <div>
      <pre><span class="c"># ① 엔진을 받는다</span>
git clone \
  https://github.com/proofer-tech/dira.git \
  ~/Projects/dira

<span class="c"># ② 워커 하나. 놓인 자리가 곧 큐의 루트다</span>
mkdir -p ~/myproject/.dira/workers
cat &gt; ~/myproject/.dira/workers/w1.sh &lt;&lt;'EOF'
#!/bin/bash
. "$HOME/Projects/dira/tick.sh"
EOF
chmod +x ~/myproject/.dira/workers/w1.sh

<span class="c"># ③ cron 두 줄</span>
* * * * * ~/myproject/.dira/workers/w1.sh
* * * * * sleep 30; ~/…/w1.sh</pre>
    </div>
    <div>
      <ul class="marks">
        <li><b>루트를 어디에도 적지 않는다.</b> <code>workers/</code>의 부모가 큐다</li>
        <li><b>두 줄인 이유는 cron의 최소 단위가 분이라서다.</b> 30초 폴링을 그렇게 낸다.
        한 줄에 <code>;</code>로 붙이면 안 된다 — 워커는 동기 프로세스라 뒷반쪽이
        30초 뒤가 아니라 앞 세션이 끝난 뒤에 뜬다</li>
        <li><b>중지는 그 두 줄을 지우는 것이다</b></li>
        <li>돌리기 전에 <code>w1.sh dryrun</code>으로 선정 결과와 프롬프트만 먼저 본다</li>
      </ul>
      <p class="arrows">
        <a href="/docs/install">전체 설치 가이드</a>
        <a href="/docs/first-ticket">첫 티켓 굴리기</a>
      </p>
    </div>
  </div>
</section>
```

**cron으로 돌릴 때는 헤드리스 인증과 전체 디스크 접근 권한이 더 필요하다.** 랜딩에 그 절차를 넣지 않는다 — `/docs/auth`로 보낸다. `.arrows`에 그 링크를 하나 더 단다:

```html
<a href="/docs/auth">cron 인증 설정</a>
```

- [ ] **Step 3: SECTION 7 — 플랜을 넣는다**

**이메일 폼을 넣지 않는다.** 폼 하나가 이메일을 개인정보 수집 항목으로 만들고 보유기간·처리위탁·수신동의를 방침에 줄줄이 붙인다. 스펙 §플랜 섹션 참고.

```html
<section class="wrap">
  <p class="eyebrow">플랜</p>
  <h2>지금은 전부 무료다</h2>
  <p class="body" style="max-width:40em">
    엔진도 앱도 MIT다. 계정도, 서버도, 요금도 없다.
    <b>여럿이 한 큐를 같이 쓰는 방법을 준비 중이다</b> — 날짜도 가격도 아직 약속하지 않는다.
  </p>
  <div class="cta" style="justify-content:flex-start; margin-top:24px">
    <a class="btn" href="https://github.com/proofer-tech/dira">GitHub에서 Watch</a>
  </div>
</section>
```

- [ ] **Step 4: 빌드하고 확인한다**

```bash
cd apps/site && pnpm build
```

기대: 성공. `/docs/install`·`/docs/first-ticket`·`/docs/auth`는 아직 없지만 Task 2 Step 6의 유예가 덮는다. **`/docs/` 밖의 링크에서 깨지면 그건 진짜 오타다** — 유예 범위를 넓히지 말고 고친다.

- [ ] **Step 5: 커밋**

```bash
git add apps/site
git commit -m "feat(랜딩 §30초 §60초 §플랜): 처음 오는 사람이 용어를 만나는 층을 끼운다

기존 페이지는 히어로 다음이 곧바로 4기둥이었는데 그 첫 단어가 티켓·워커·큐다.
셋 다 dira의 고유 어휘지 일반 어휘가 아니다 — 정의가 먼저 와야 한다.

플랜 섹션에 이메일 폼을 넣지 않는다. 방침이 짧게 끝나는 이유가 수집 항목이 랜덤
UUID 하나이기 때문인데, 폼 하나가 보유기간·처리위탁·수신동의를 줄줄이 붙인다."
```

---

### Task 4: 랜딩 나머지 섹션과 기존 페이지 폐기

**Files:**
- Modify: `apps/site/.vitepress/theme/Landing.vue` (SECTION 3·4·6)
- Delete: `docs/index.html`

- [ ] **Step 1: SECTION 3 — 4기둥을 옮긴다**

`docs/index.html:238-259`를 그대로 옮긴다. 카피 한 줄만 바꾼다 — 엔진 기둥의 `설치는 git clone이 끝이고`는 이제 §60초 설치와 겹치므로 이렇게 바꾼다:

```html
<p>그 밖으로 나가지 않는다. 상주 루프도, DB도, 인덱스도 없다.</p>
```

- [ ] **Step 2: SECTION 4 — 실제 화면을 옮긴다**

`docs/index.html`의 네 `<section>`(276–400줄)에서 **그림만** 가져온다. 긴 설명 텍스트는 매뉴얼로 내린다(스펙 §랜딩).

`barge.gif`(299줄) · `07-qa-thread.png`(334줄) · `04-ticket-running.png`(458줄) 셋을 `figure`로 나란히 두고, 각 `figcaption`은 원본을 그대로 쓴다. 각 그림 아래 `.arrows`로 매뉴얼의 해당 장을 건다:

| 그림 | 링크 |
|---|---|
| `barge.gif` | `/docs/barge-in` |
| `07-qa-thread.png` | `/docs/ticket-writing` |
| `04-ticket-running.png` | `/docs/states` |

- [ ] **Step 3: stats를 옮기되 날짜를 박는다**

`docs/index.html:267-274`를 옮긴다. **숫자는 드리프트한다** — 이미 `405 티켓/완료 397`과 아래 figcaption의 `완료 387건`이 어긋나 있다. 빌드 때 계산하지 않고 **스냅숏임을 명시한다**:

```html
<div class="wrap stats">
  <ul>
    <li><b>0</b><span>엔진 의존성<br>bash + python3 표준 라이브러리</span></li>
    <li><b>6</b><span>이 레포에서 동시에 도는 워커</span></li>
    <li><b>405</b><span>자기 큐가 처리한 티켓<br>완료 397</span></li>
    <li><b>4일</b><span>첫 커밋에서 첫 릴리스까지<br>커밋 389</span></li>
  </ul>
  <p class="stats-note">2026-08-01 기준</p>
</div>
```

넷째 칸의 `v0.1.2까지`를 `첫 릴리스까지`로 바꾼다 — 그래야 버전이 올라도 사실로 남는다.

```css
.stats-note { margin: 0; padding: 0 0 16px; font-size: 11.5px; font-family: var(--mono); color: var(--faint); }
```

- [ ] **Step 4: SECTION 6 — 여기 없는 것을 그대로 옮긴다**

`docs/index.html:465-481`. **한 줄도 고치지 않는다.** 이 제품의 톤이 여기 있다.

- [ ] **Step 5: 기존 페이지를 지운다**

```bash
git rm docs/index.html
```

- [ ] **Step 6: 빌드하고 커밋**

```bash
cd apps/site && pnpm build
```

```bash
git add apps/site docs
git commit -m "feat(랜딩 §4기둥 §화면 §여기 없는 것): 옮기고 docs/index.html을 걷는다

긴 설명 섹션 넷(세션에 말을 건다·스스로 잠근다·워커를 하나 더·큐는 여럿)은 그림만
남기고 본문을 매뉴얼로 내린다. 랜딩에 깊이가 있으면 결정이 늦어지고, 그 내용은
이제 갈 데가 생겼다.

stats에 '2026-08-01 기준'을 박는다. 405/397은 이미 아래 figcaption의 387과 어긋나
있었다 — 빌드 때 세는 대신 스냅숏임을 밝힌다. 넷째 칸의 'v0.1.2까지'는
'첫 릴리스까지'로 바꾼다. 그래야 버전이 올라도 사실로 남는다."
```

---

### Task 5: 매뉴얼 뼈대와 시작하기 3장

**Files:**
- Create: `apps/site/docs/index.md`, `what-is-dira.md`, `install.md`, `first-ticket.md`
- Modify: `apps/site/.vitepress/config.ts` (사이드바)

- [ ] **Step 1: 사이드바를 넣는다**

`config.ts`의 `themeConfig`에 더한다. **18장 전부를 지금 적는다** — 파일이 없으면 빌드가 깨지므로 Task 6·7·8이 채울 때까지 그 항목은 주석 처리하고, 각 Task가 자기 것을 푼다.

```ts
nav: [
  { text: "매뉴얼", link: "/docs/" },
  { text: "다운로드", link: "https://github.com/proofer-tech/dira/releases/latest" },
],
sidebar: {
  "/docs/": [
    {
      text: "시작하기",
      items: [
        { text: "dira가 뭔가", link: "/docs/what-is-dira" },
        { text: "설치", link: "/docs/install" },
        { text: "첫 티켓 굴리기", link: "/docs/first-ticket" },
      ],
    },
    // 설정 — Task 6
    // 쓰기 — Task 7
    // 운영·레퍼런스 — Task 8
  ],
},
socialLinks: [{ icon: "github", link: "https://github.com/proofer-tech/dira" }],
editLink: {
  pattern: "https://github.com/proofer-tech/dira/edit/master/apps/site/:path",
  text: "이 페이지 고치기",
},
```

- [ ] **Step 2: `docs/index.md` — 매뉴얼 홈**

18장을 4부로 묶은 목차 한 장. 처음 온 사람에게 **"여기부터"**를 명시한다: `dira가 뭔가` → `설치` → `첫 티켓 굴리기` 셋을 순서대로 읽으면 30분 안에 첫 티켓이 돈다고 적는다.

- [ ] **Step 3: `what-is-dira.md` — 5분**

출처는 `README.md` 머리말. 반드시 담을 것:
- 한 문장 정의 — 파일시스템이 곧 큐인 티켓 디스패처
- 이름의 유래 (`dir` + `jira`)
- 용어 4개를 각각 한 문단으로: **티켓**(마크다운 파일 하나) · **큐**(그 파일들의 디렉터리) · **워커**(cron에 걸린 셸 스크립트, 실행 1회에 티켓 1건) · **세션**(워커가 띄운 `claude -p` 프로세스)
- 엔진과 앱의 구분 — 앱은 선택이고 엔진만으로 돈다
- **안 하는 것** — 서버 배포 없음, 인증·멀티유저 없음, 앱은 macOS(Apple Silicon)뿐, 우선순위 없음

- [ ] **Step 4: `install.md` — 세 갈래**

출처는 `README.md §요구사항 §설치`. 갈래를 먼저 고르게 한다:

| 갈래 | 받는 것 |
|---|---|
| 맥 앱 + 엔진 | 릴리스에서 `.dmg`. 앱이 엔진을 같이 들고 있다 |
| 엔진만 | `git clone` 하나. 화면 없이 cron으로만 돈다 |
| 둘 다 손으로 | 개발자용 |

담을 것:
- 요구사항 — macOS 또는 Linux, `python3`(표준 라이브러리만), `bash`, 에이전트 CLI 1종
- `git clone https://github.com/proofer-tech/dira.git ~/Projects/dira` — **하이픈 있는 `proofer-tech`**
- `tick.sh`는 직접 실행하지 않는다. 진입점은 워커다
- 다음은 `/docs/first-ticket`

- [ ] **Step 5: `first-ticket.md` — 10분 튜토리얼**

출처는 `README.md §빠른 시작`. **손을 잡고 끝까지 간다.** 순서:
1. 워커 파일 하나 만들기 (`mkdir -p` → `cat > w1.sh` → `chmod +x`)
2. (선택) `cp -r templates/* .dira/` — 엔진은 없어도 돈다고 명시
3. 티켓 하나 쓰기 — `README §빠른 시작`의 `a1b2c3d4.md` 예제 그대로
4. **`w1.sh dryrun`** — 실행 없이 선정 결과와 프롬프트만 본다. 여기서 먼저 확인시킨다
5. `w1.sh`를 손으로 한 번 돌리기
6. `w1.sh list`로 결과 보기
7. cron 등록은 아직 안 한다 → `/docs/cron`으로 보낸다

- [ ] **Step 6: 빌드하고 커밋**

```bash
cd apps/site && pnpm build
```

```bash
git add apps/site
git commit -m "docs(매뉴얼 §시작하기): 30분 안에 첫 티켓이 도는 길 셋

dira가 뭔가 → 설치 → 첫 티켓. 첫 장에서 용어 넷(티켓·큐·워커·세션)을 먼저 세운다.
튜토리얼은 cron 등록 전에 dryrun과 손 실행으로 끊는다 — cron은 인증과 권한이
더 필요해서 여기서 붙이면 첫 성공이 그 벽에 걸린다."
```

---

### Task 6: 설정 4장

여기가 **처음 쓰는 사람이 실제로 막히는 자리**다. cron이 조용히 안 도는 원인 대부분이 이 4장에 있다.

**Files:**
- Create: `apps/site/docs/worker.md`, `cron.md`, `auth.md`, `concurrency.md`
- Modify: `apps/site/.vitepress/config.ts` (사이드바 `설정` 그룹 활성화)

- [ ] **Step 1: `worker.md` — 워커 만들기**

출처는 `README.md §빠른 시작` `§티켓` · `worker.sh.example`. 담을 것:
- **워커가 놓인 위치가 곧 티켓 루트다.** `workers/`의 부모가 루트. 경로를 어디에도 적지 않는다
- 큐를 프로젝트 밖(공유 클라우드)에 두는 법과, 마운트가 안 붙으면 워커 파일 자체가 없어 cron이 조용히 실패한다는 사실 — **빈 큐로 오해하고 도는 일이 없다**
- 세션의 작업 디렉터리는 기본이 루트의 부모. 바꾸려면 `TICKET_CWD`
- 엔진 바꾸기 — 워커 한 줄

- [ ] **Step 2: `cron.md` — 두 줄 규칙**

출처는 `README.md §빠른 시작`의 cron 항. 이 장의 핵심 한 가지:

> **한 줄에 `;`로 붙이지 않는다.** 워커는 동기 프로세스라 앞 호출이 세션을 물면 뒷반쪽이 30초 뒤가 아니라 **그 세션이 끝난 뒤**에 뜬다. 두 줄이어야 :00·:30이 결정적이다. 중복 디스패치는 워커 락이 막는다.

그 밖에: 왜 두 줄인가(cron 최소 단위가 분) / 중지는 두 줄 삭제 / 로그 자리(`workers/runner.log`, `workers/logs/<시각>-<워커>-<해시>.log`)

- [ ] **Step 3: `auth.md` — 헤드리스 인증과 권한**

출처는 `README.md §요구사항 §설치`. **cron으로 돌릴 때만 필요하다**고 먼저 못박는다. 담을 것:
- **전체 디스크 접근 권한** — 티켓 루트가 클라우드 마운트일 때 필수
- **장기 인증 토큰** — cron은 로그인 키체인에 접근하지 못한다

```bash
mkdir -p ~/.config/dira
claude setup-token
printf %s '<토큰>' > ~/.config/dira/oauth-token && chmod 600 ~/.config/dira/oauth-token
```

- **토큰은 머신 로컬에 하나만 둔다.** 티켓 루트가 공유 드라이브여도 비밀이 같이 동기화되지 않게
- **워커를 만들 때마다 macOS가 `앱 관리` 권한을 묻는다.** 승인이 다음 등록까지 남지 않는다
- 새 스크린샷 하나 — macOS `설정 › 개인정보 보호 및 보안 › 전체 디스크 접근 권한`. `apps/site/public/shots/05-full-disk-access.png`로 넣는다

- [ ] **Step 4: `concurrency.md` — 동시성 = 워커 개수**

출처는 `README.md §빠른 시작` 마지막 항. 담을 것:
- 동시 실행 상한 같은 설정값이 **없다**. 워커를 하나 더 두면 하나 더 돈다
- 워커 하나는 한 번에 티켓 1건만 물고(락), 앞 실행이 세션을 쥐고 있으면 그 분의 tick은 넘어간다
- `w2.sh`를 만들고 cron 두 줄 더
- 세션 실행 상한은 `TICKET_MAXRUN` 하나. 넘으면 백로그로 돌아간다

- [ ] **Step 5: 사이드바 `설정` 그룹을 푼다**

```ts
{
  text: "설정",
  items: [
    { text: "워커 만들기", link: "/docs/worker" },
    { text: "cron 등록", link: "/docs/cron" },
    { text: "헤드리스 인증", link: "/docs/auth" },
    { text: "동시성", link: "/docs/concurrency" },
  ],
},
```

- [ ] **Step 6: 빌드**

```bash
cd apps/site && pnpm build
```

기대: 성공. `ignoreDeadLinks` 유예는 아직 켜 둔 채다 — Task 8이 18장을 다 채운 뒤 되돌린다.

- [ ] **Step 7: 커밋**

```bash
git add apps/site
git commit -m "docs(매뉴얼 §설정): 처음 쓰는 사람이 실제로 막히는 네 자리

cron이 조용히 안 도는 원인이 여기 다 있다 — 한 줄에 ;로 붙이기, 키체인에 못 붙는
헤드리스, 마운트 없는 큐 루트, 워커마다 다시 묻는 앱 관리 권한.

cron 장은 두 줄 규칙 하나에 지면을 쓴다. ;로 붙이면 뒷반쪽이 30초 뒤가 아니라
앞 세션이 끝난 뒤에 떠서 폴링이 폴링이 아니게 된다."
```

---

### Task 7: 쓰기 5장

**Files:**
- Create: `apps/site/docs/ticket-writing.md`, `states.md`, `screens.md`, `barge-in.md`, `personas.md`
- Modify: `apps/site/.vitepress/config.ts` (사이드바 `쓰기` 그룹)

- [ ] **Step 1: `ticket-writing.md`**

출처는 `README.md §티켓` · `templates/`. 담을 것: `kind` 셋(`work`·`request`·`feedback`)의 차이 / `Goal`과 `Done when` 쓰는 법 / `deps`로 순서 만들기 / 페르소나 지정 / 좋은 티켓과 나쁜 티켓 예시 한 쌍

- [ ] **Step 2: `states.md`**

출처는 `README.md §티켓` 상태 표. 담을 것: **상태는 파일명 접미사, 나머지는 frontmatter** / 접미사 표 전부 / `session_id`·`pid`·`owner`·`inbox`는 **디스패처가 쓰고 사람은 건드리지 않는다** / 잡기가 `os.link` 한 번인 이유(판정과 획득이 한 시스템콜, Maildir 계보)

- [ ] **Step 3: `screens.md`**

출처는 `docs/DESIGN.md` · 스크린샷 4장. 담을 것: 프로젝트 목록(등록·해제, **해제해도 큐 파일은 그대로**) / 보드 세 레인 / 티켓 상세 / 워커 화면(pid가 있는 이유 — 회수 판정이 시간이 아니라 프로세스 생존) / 설정 다이얼로그

- [ ] **Step 4: `barge-in.md` — 도는 세션에 말 걸기**

출처는 `docs/index.html:276-309`에서 내린 본문 + `DESIGN.md`. 담을 것:
- **턴이 끝나기를 기다리지 않는다.** 스트림 아래 칸에 한 줄 넣으면 그 세션의 stdin으로 들어간다
- 입구는 세션마다 FIFO 하나. 디스패처가 만들고 지운다
- 스트림 원본은 claude가 이미 쓰고 있는 트랜스크립트다 — 새로 남기는 파일이 없다
- 참견은 화면이 지어낸 에코가 아니라 **트랜스크립트에 실제로 남은 줄**이다
- 창은 세션이 도는 동안이다. 턴이 끝나면 닫힌다
- **에이전트가 스스로 잠그는 것**(`docs/index.html:311-340`에서 내린 본문): 되묻기 = 존재하지 않는 파일을 dep으로 걸기 / 답을 쓰면 그 파일이 생겨 잠금이 풀린다 / 2회까지 자동 회수, 3회째는 질문이 된다
- `barge.gif` 삽입

- [ ] **Step 5: `personas.md`**

출처는 `templates/` · `README.md §티켓`. 담을 것: `personas/<이름>/PROFILE.md`는 **누가** / `protocols/AGENTS.md`는 **어떻게 같이 일하는가**(전원 공통) / 템플릿은 출발점이고 **엔진은 이게 없어도 그대로 돈다** / 티켓이 페르소나를 고른다

- [ ] **Step 6: 사이드바와 빌드**

```ts
{
  text: "쓰기",
  items: [
    { text: "티켓 쓰는 법", link: "/docs/ticket-writing" },
    { text: "상태는 파일명", link: "/docs/states" },
    { text: "화면에서 하는 일", link: "/docs/screens" },
    { text: "도는 세션에 말 걸기", link: "/docs/barge-in" },
    { text: "페르소나와 프로토콜", link: "/docs/personas" },
  ],
},
```

```bash
cd apps/site && pnpm build
```

- [ ] **Step 7: 커밋**

```bash
git add apps/site
git commit -m "docs(매뉴얼 §쓰기): 랜딩에서 내린 본문이 갈 데가 생겼다

세션에 말을 건다 · 에이전트가 스스로 잠근다 두 섹션의 본문을 barge-in.md로 옮긴다.
랜딩에는 그림과 한 문장만 남는다."
```

---

### Task 8: 운영 3장 + 레퍼런스 3장

**Files:**
- Create: `apps/site/docs/troubleshooting.md`, `logs.md`, `analytics.md`, `ref-env.md`, `ref-cli.md`, `ref-frontmatter.md`
- Modify: `apps/site/.vitepress/config.ts`

- [ ] **Step 1: `troubleshooting.md` — 증상에서 시작한다**

**증상 → 원인 → 확인 명령** 형식이다. 최소 이 여섯:

| 증상 | 원인 |
|---|---|
| cron이 도는데 아무 일도 안 일어난다 | 헤드리스 토큰 없음/만료 (`/docs/auth`) |
| 워커가 아예 안 뜬다 | 티켓 루트가 클라우드 마운트인데 안 붙음 — 워커 파일 자체가 없어 cron이 조용히 실패 |
| 전체 디스크 접근 권한을 줬는데도 안 된다 | 워커를 새로 만들 때마다 macOS가 `앱 관리`를 다시 묻는다 |
| 티켓이 진행중에 멈춰 있다 | 스테일 수거 — 회수는 시간이 아니라 프로세스 생존으로 판정한다 |
| 같은 티켓이 계속 죽는다 | 2회까지 자동 회수, 3회째는 질문이 된다 |
| 두 워커가 같은 티켓을 문다 | 일어나지 않는다. `os.link`가 `EEXIST`로 막는다. 그렇게 보이면 워커 락 로그를 본다 |

- [ ] **Step 2: `logs.md`**

`workers/runner.log`(디스패치 기록) · `workers/logs/<시각>-<워커>-<해시>.log`(세션별 출력) · `workers/cron.log`(cron 리다이렉트). 각각 무엇이 들어가고 무엇을 볼 때 여는지.

- [ ] **Step 3: `analytics.md` — 사용 통계와 끄는 법**

출처는 `docs/DESIGN.md §0-11`. 담을 것:
- 무엇을 보내는가 — 이벤트 8종 표
- **무엇을 안 보내는가** — 경로·프로젝트명·큐 루트·티켓 해시/제목/본문·페르소나명·에이전트 출력
- 끄는 법 — `설정` 다이얼로그 세 번째 섹션
- 지우는 법 — `~/.config/dira/analytics.json`
- **자격값이 없으면 아무것도 안 보낸다** — `pnpm dev`와 손으로 빌드한 `.app`의 정상 상태
- `/privacy`로 링크

- [ ] **Step 4: 레퍼런스 3장**

`ref-env.md`(`README §워커 레퍼런스`의 환경변수 표 전부) · `ref-cli.md`(`list`·`dryrun`·`reap` — `tick.sh`·`tickets.py`에서 실제 서브커맨드를 확인하고 적는다) · `ref-frontmatter.md`(필드 표 전부, 사람이 쓰는 것과 디스패처가 쓰는 것을 갈라서)

- [ ] **Step 5: 사이드바를 채우고 죽은 링크 유예를 되돌린다**

Task 2 Step 6에서 켠 `ignoreDeadLinks: [/^\/docs\//]`를 **`ignoreDeadLinks: false`로 되돌린다.** 18장이 다 들어왔으므로 이제부터는 매뉴얼 내부 링크도 빌드가 검증한다.

```ts
{
  text: "운영",
  items: [
    { text: "트러블슈팅", link: "/docs/troubleshooting" },
    { text: "로그 읽는 법", link: "/docs/logs" },
    { text: "사용 통계와 끄는 법", link: "/docs/analytics" },
  ],
},
{
  text: "레퍼런스",
  items: [
    { text: "워커 환경변수", link: "/docs/ref-env" },
    { text: "CLI", link: "/docs/ref-cli" },
    { text: "frontmatter 필드", link: "/docs/ref-frontmatter" },
  ],
},
```

```bash
cd apps/site && pnpm build
```

기대: 성공. `ignoreDeadLinks: false` 상태에서 18장 전부의 내부 링크가 검증된다.

- [ ] **Step 6: 커밋**

```bash
git add apps/site
git commit -m "docs(매뉴얼 §운영 §레퍼런스): 증상에서 시작하는 트러블슈팅

원인이 아니라 증상으로 목차를 짠다 — 처음 쓰는 사람은 '헤드리스 토큰이 만료됐다'가
아니라 'cron이 도는데 아무 일도 안 일어난다'를 들고 온다."
```

---

### Task 9: 이용약관·개인정보처리방침과 코드 잠금

방침의 이벤트 표가 `analytics.ts`와 갈리면 테스트가 깨진다. **문구가 아니라 이행 수단이다.**

**Files:**
- Create: `apps/site/terms.md`
- Create: `apps/site/privacy.md`
- Create: `apps/site/privacy.test.ts`

- [ ] **Step 1: 방침-코드 잠금 테스트를 먼저 쓴다**

`apps/site/privacy.test.ts`:

```ts
/** 개인정보처리방침의 이벤트 표는 `apps/teams/lib/analytics.ts`의 `Events` 타입이 원본이다.
 *  이벤트를 늘리고 방침을 안 고치면 여기서 깨진다 — 그것이 이 테스트의 전부다.
 *  §0-11이 "타입이 표를 닫는다"고 한 것을 방침까지 이어 붙인다. */
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

function eventNames(): string[] {
  const src = readFileSync(
    new URL("../teams/lib/analytics.ts", import.meta.url),
    "utf8",
  );
  const block = src.match(/export type Events = \{([\s\S]*?)\n\};/);
  assert.ok(block, "analytics.ts에서 `export type Events` 블록을 못 찾았다");
  return [...block[1].matchAll(/^ {2}(\w+):/gm)].map((m) => m[1]);
}

test("방침의 이벤트 표가 Events 타입을 다 담는다", () => {
  const events = eventNames();
  assert.equal(events.length, 8, `Events가 8개가 아니다: ${events.join(", ")}`);

  const privacy = readFileSync(new URL("./privacy.md", import.meta.url), "utf8");
  for (const name of events) {
    assert.ok(
      privacy.includes(`\`${name}\``),
      `방침에 \`${name}\` 이벤트가 빠졌다 — 표를 고쳐라`,
    );
  }
});

test("방침이 안 보내는 항목을 명시한다", () => {
  const privacy = readFileSync(new URL("./privacy.md", import.meta.url), "utf8");
  for (const forbidden of ["티켓 본문", "파일 경로", "프로젝트 이름", "페르소나"]) {
    assert.ok(privacy.includes(forbidden), `방침 §처리하지 않는 항목에 "${forbidden}"이 없다`);
  }
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/site && node --test "*.test.ts"
```

기대: `privacy.md`가 없어 `ENOENT`로 실패.

- [ ] **Step 3: `privacy.md`를 쓴다**

스펙 §개인정보처리방침을 그대로 옮긴다. 절 순서:

1. 처리 목적
2. 처리 항목 — 설치 식별자 / 세션 식별자 / **이벤트 8종 표**(`app_open`·`screen_view`·`project_add`·`worker_create`·`ticket_create`·`answer_submit`·`feedback_submit`·`analytics_off`) / 웹사이트 Vercel Analytics 집계값
3. **처리하지 않는 항목** — 파일 경로 · 프로젝트 이름 · 큐 루트 · 티켓 해시 · 티켓 제목 · 티켓 본문 · 페르소나 이름 · 에이전트 출력 · 계정 정보 · 소스 코드. `analytics.ts`의 `Events` 타입 링크를 건다
4. 쿠키 — **앱도 웹사이트도 설치하지 않는다.** 그래서 동의 배너가 없다
5. 보유·파기 — 아래 표 그대로 / 로컬 `analytics.json`은 지우면 소멸
6. 처리위탁·국외이전 — Google LLC(GA4, 미국) · Vercel Inc.(호스팅·통계, 미국). 제3자 제공 없음
7. 정보주체 권리 — 끄기(설정 다이얼로그) · 삭제(`~/.config/dira/analytics.json`) · 문의 info@proofer.tech
8. 개인정보 보호책임자 — **임한솔(대표) · info@proofer.tech**
9. 사업자 정보 — 프루퍼 주식회사 / 대표 임한솔 / 337-81-03650 / 서울 강남구 강남대로112길 47, 2층 421A
10. 시행일 — 2026-08-02

§보유·파기의 표는 이 값 그대로다 (2026-08-02 GA4 콘솔 확인):

```markdown
| 대상 | 보존 기간 |
|---|---|
| 이벤트 데이터 — 이벤트 8종과 그 파라미터 | 2개월 |
| 사용자 데이터 — 설치 식별자에 연결된 데이터 | 14개월 |
```

**둘을 하나로 뭉뚱그리지 않는다** — GA4가 따로 잡는 값이고, 실제로 다르다.

- [ ] **Step 4: 통과를 확인한다**

```bash
cd apps/site && node --test "*.test.ts"
```

기대: 3 pass (버전 1 + 방침 2).

- [ ] **Step 5: `terms.md`를 쓴다**

절 순서:
1. **이 도구가 무엇을 하는가** — dira는 에이전트 CLI에게 **사용자의 파일을 읽고 고치게** 하는 도구다. 어떤 디렉터리를 큐 루트로 삼을지, 무엇을 커밋할지의 판단과 결과는 사용자에게 있다. **면책 문구이기 전에 사실 고지다**
2. 라이선스와 무보증 — MIT 그대로
3. 서드파티 — `claude` 등 에이전트 CLI의 약관은 사용자와 그 제공자 사이의 것이다. dira는 그 사이에 서지 않는다
4. 요금 — 현재 없음. 유료가 생기면 사전 고지
5. 변경 — 이 페이지 갱신으로 고지. 변경일을 상단에 적는다
6. 사업자 정보 — 방침과 같은 표
7. 시행일 — 2026-08-02

**통신판매업 신고번호를 적지 않는다** — 판매가 없다.

- [ ] **Step 6: 빌드하고 커밋**

```bash
cd apps/site && pnpm test && pnpm build
```

```bash
git add apps/site
git commit -m "docs(약관·방침): 이벤트 표를 Events 타입에 잠근다

privacy.test.ts가 analytics.ts의 Events 타입에서 이름 8개를 뽑아 방침 본문에
전부 있는지 본다. 이벤트를 늘리고 방침을 안 고치면 CI가 깨진다 — §0-11이
'타입이 표를 닫는다'고 한 것을 방침까지 이어 붙인 것이다.

약관 1조는 면책이 아니라 사실 고지다: 이건 에이전트에게 당신의 파일을 고치게
하는 도구다.

GA4 보존 기간 한 줄이 비어 있다(스펙 §열린 항목). 공개 전에 채운다."
```

---

### Task 10: CI와 Vercel 배포

**Files:**
- Modify: `.github/workflows/ci.yml`
- Create: `apps/site/vercel.json`

- [ ] **Step 1: CI에 site를 더한다**

`.github/workflows/ci.yml`의 `apps/desktop` 블록 뒤에 붙인다. **잡을 쪼개지 않는다** — 기존 판단(`ponytail: 잡 하나에 8개를 다 넣는다`) 그대로다.

```yaml
      # 랜딩·매뉴얼·약관. build가 죽은 내부 링크에서 깨지므로 링크 검사가 여기 붙어 있다.
      - run: pnpm install
        working-directory: apps/site
      - run: pnpm test
        working-directory: apps/site
      - run: pnpm build
        working-directory: apps/site
```

- [ ] **Step 2: CI가 도는지 로컬에서 흉내낸다**

```bash
cd apps/site && pnpm install && pnpm test && pnpm build
```

기대: 3 pass, 빌드 성공.

- [ ] **Step 3: Vercel 설정을 넣고 커밋**

`apps/site/vercel.json`:

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "outputDirectory": ".vitepress/dist",
  "framework": null
}
```

```bash
git add .github apps/site
git commit -m "ci(apps/site): 설치·테스트·빌드 세 줄을 기존 잡에 붙인다

잡을 쪼개지 않는다 — ci.yml의 기존 판단 그대로다. VitePress build가 죽은 내부
링크에서 깨지므로 매뉴얼 18장의 링크 검사가 이 세 줄에 같이 붙어 있다."
```

- [ ] **Step 4: 사람이 하는 것 — Vercel 프로젝트와 도메인**

**여기서 멈추고 사용자에게 넘긴다.** 이 단계는 코드가 아니다.

1. Vercel에서 `proofer-tech/dira` 레포로 새 프로젝트를 만든다
2. Root Directory를 `apps/site`로 잡는다
3. Framework Preset은 `Other`, Build `pnpm build`, Output `.vitepress/dist`
4. Domains에 `dira.proofer.tech`를 더한다
5. Analytics 탭에서 **Web Analytics를 켠다**(쿠키리스)

**공개 전 최종 확인 6가지:**

- [ ] `dira.proofer.tech`가 열린다
- [ ] 히어로 CTA 둘이 각각 릴리스·`/docs/install`로 간다
- [ ] 랜딩의 버전이 `apps/desktop/package.json`의 현재 값과 같다
- [ ] `/terms`·`/privacy`가 푸터에서 닿는다
- [ ] **DevTools › Application › Cookies가 비어 있다** — 랜딩·매뉴얼·약관 전부
- [ ] README의 `git clone`을 그대로 복사해 붙여넣으면 실제로 받아진다

---

## 남은 것

**없다.** 스펙의 열린 항목이 둘 다 닫혔다 — 개인정보 보호책임자는 임한솔(대표), GA4 보존
기간은 이벤트 2개월 · 사용자 14개월이다. Task 1부터 순서대로 실행하면 된다.
