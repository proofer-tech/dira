# dira

**파일시스템이 곧 큐인 티켓 디스패처이고, 이 레포는 그 큐로 자기 자신을 만든다**(도그푸딩).
엔진은 `tick.sh`·`tickets.py` 둘뿐이고 의존성이 0이다 — 상주 루프도 DB도 인덱스도 없이,
상태의 단일 출처가 티켓 파일 자체(파일명 접미사 + frontmatter)다.

이름은 `dir` + `jira`다. 큐가 디렉터리 하나라서 `dir`이고, 그 디렉터리를 티켓으로 보는
도구라서 `jira`의 오마주다.

## 무엇이 어디 사나

| 자리 | 무엇 |
|---|---|
| `tick.sh` `tickets.py` | 엔진. bash + python3 표준 라이브러리만. 워커가 cron에서 이걸 물고 티켓 1건을 헤드리스 에이전트 세션에 넘긴다 |
| `protocols/CORE.md` `protocols/CORE-TICKETS.md` | **어느 프로젝트에나 실리는** 큐 규약. 어기면 결과물이 아니라 큐가 망가진다 |
| `templates/` | 새 프로젝트가 복사해 가는 원본 — 협업 프로토콜(`protocols/AGENTS.md`)과 페르소나 프로필(`personas/<이름>/PROFILE.md`). 큐에 도는 사본이 따로 있다 → [[프로토콜 두 벌]] |
| `docs/DESIGN.md` | GUI(`dira teams`) 제품 스펙의 **단일 출처**. 요구·결정·로드맵이 전부 여기. 값의 정본은 한 표에 있어도 그 값을 인용하는 산문은 흩어져 있다 → [[스펙의 낡은 인용]] |
| `apps/teams/` | 큐를 보는 로컬 Next.js 웹 UI. 엔진과 격리돼 있고 의존성 0 약속의 바깥이다 → [[단축키 기본값]] |
| `apps/desktop/` | 그 UI를 감싸는 Electron 껍데기. **버전 정본이 여기 `package.json` 하나**이고 릴리스가 그 값을 origin에서 올린다 → [[origin과의 갈래]] |
| `apps/site/` | 랜딩 · 매뉴얼(VitePress) |
| `ontology/프로토콜/` | 끝난 티켓에서 꺼낸 개념 중 **어느 프로젝트에나 실리는 층**의 것 — 큐 규약과 아카이빙 |
| `ontology/GUI/` | 같은 개념 중 **이 레포의 제품**(`apps/teams/`)의 것 |

**개념 파일은 이 문서와 한 vault다.** 파일 하나가 개념 하나이고(→ [[개념 하나에 파일 하나]])
`[[링크]]`가 경로도 확장자도 없이 서로 닿고, **디렉터리 트리는 그 위에 얹힌 또 하나의 축이다**
→ [[온톨로지 계층]]. 위 두 자리는 지금의 개념이 갈린 결과이지 정해진 축이 아니다 — 이름이
유일해서 다시 갈라도 링크를 한 줄도 안 고친다.

## 큐는 이 레포에 없다

`.dira/`는 gitignore돼 있다 — 큐는 git에 안 들어가는 것이 불변식이다. 그래서 clone한 사람에게
보이는 것은 위 표가 전부이고, **끝난 티켓의 지식은 `ontology/`와 이 문서로만 건너온다.**
그 옮기는 일을 하는 것이 [[아카이브 티켓]]이다.

**반대도 참이라 티켓에서 한 번 샜다 — `ontology/`는 큐에 없다.** 티켓이 개념 파일을 근거로
인용하면 받는 세션의 손은 `.dira/` 아래로 가고, 거기 없으니 인용이 통째로 버려진다
→ [[지적을 축으로 쪼갠다]].

관련: [[프로토콜 두 벌]] · [[아카이브 티켓]] · [[온톨로지 계층]] · [[개념 하나에 파일 하나]] · [[메모리 예산]] · [[단축키 기본값]] · [[모션 예외]] · [[줄 안의 표식]] · [[안 갈렸다는 증명]] · [[스펙의 낡은 인용]] · [[지적을 축으로 쪼갠다]] · [[origin과의 갈래]]
