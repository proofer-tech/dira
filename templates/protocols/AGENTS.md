# 협업 프로토콜

큐는 파일이고 파일이 곧 상태다. **망가뜨리면 당신의 다음 티켓이 사라진다.** 이 문서는
**이 프로젝트의 관습**이고 **상황부 세부는 형제 문서에 있다** - 그 상황을 만나면 그 파일부터
읽는다.

## 인라인 예산 - 이 파일 6,500 B

이 문서는 **매 디스패치 전원의 프롬프트에 실린다.** 이 절이 상한의 정본이고 셋이다 - 큐
`protocols/AGENTS.md` **6,500 B**, 코어 `CORE.md` **3,500 B**, 페르소나당 `PROFILE.md`+
`skills.md` 합 **5,000 B**. `wc -c`로 잰다(페르소나는 두 파일 합).

**걸리는 순간은 넘은 채 더할 때다.** 걸리면 **자기가 지금 만지는 그 파일을 더하는 바이트보다
많이 줄이고 더한다.** 접는 것은 경위-실측-일화, 남기는 것은 임계값-명령-금지-판정 - 접은 것은 형제로 내리고 인라인엔 트리거 + 파일명 한 줄만 둔다.
**가르기는 예산을 안 갚는다.**

## 당신이 있는 곳 - git 워크트리 (워커 2개 이상)

워커가 하나면 건너뛴다 - 레포에서 일한다. 여럿이면 cwd는 `<프로젝트>/.dira/worktrees/<워커>`,
브랜치 `wt/<워커>`인 **git 워크트리**고 소스-문서는 자기 사본이라 여기서 고치고 커밋한다.
`.dira/`는 **큐 하나를 가리키는 심링크**고 gitignore라 `git status`에 티켓이 안 뜨는 게
정상이며, 조상을 가리켜 무한 재귀하니 **워크트리 루트에서 `find -L`-`grep -R` 금지.** 트리를
만드는 3줄은 `워크트리.md`.

## git - 시작 - 끝

- **시작하자마자 `git rebase <통합 브랜치>`.**
- **`## 결과`-`## 블록`이 붙어 온 티켓은 재디스패치다** - 그 글은 주장이지 검증이 아니다.
  `git log --oneline <통합 브랜치> -- <파일>`로 확인하고, 없으면 `재디스패치-복구.md`대로 한다.
- **끝나면 `bash .dira/push.sh ship <해시> "<제목>" ["<본문>"]` 한 번.** add-commit(마지막 줄
  `Ticket: <해시>` 자동)-push-non-ff 1회 재시도까지 헬퍼가 다 한다. push하지 않으면 당신의 작업은
  존재하지 않는다. 그래도 거부되면 `git rebase <통합 브랜치>` 후 재시도, 3회 실패면 `## 블록`.
  락 상한 초과-되돌리기 실패-헬퍼 없는 프로젝트는 `push-거부.md`.
- **`git commit`이 졌거나 받는 트리에서 커밋했으면 `워크트리.md`** - 처분은 진 트리로 갈리고,
  예외는 커밋 메시지 `Exception: <사유>` 한 줄이다.
- 금지: `push --force` `reset --hard` `stash` 원격(origin) push. 사람이 한다.

## 턴을 닫으면 세션이 끝난다

엔진의 감시 루프가 마지막 줄이 `result`면 그 자리에서 세션을 끝낸다 - **백그라운드로 던진
작업의 알림을 받을 창이 없다.** 띄웠으면 같은 턴 안에서 받는다(전경 또는 폴링) - `## 진행
계획`을 든 티켓이 특히 위험하다. 경위는 `세션-종료.md`. 이 금지는 세션이 살아 있는 동안
기다리는 자리를 겨눈다 - 기다리는 주체가 티켓이면 다른 길이다, `폴링-대기.md`.

## 미완으로 끝날 때 - 이어받기 한 장을 내고 닫는다

`## Done when`을 다 못 채웠는데 이번 턴으로 끝내야 한다고 **세션 자신이** 판단했으면 넘긴다.
**push가 맨 앞이고**, 이어받기 티켓 **한 장**을 낸 뒤 원본 fm에 `continued: <새 해시>`를
달고 `.done`으로 닫는다. **다섯 줄 순서와 키 표는 `세션-종료.md`.**
**`## 블록`이 아니다.** 가르는 질문 둘 - **사람이 답을 쓰면 이 자리가 풀리나**(아니오),
**지금 이 턴에 글을 쓸 수 있나**(예) - 둘 다면 이 절, 거절되면 §한도.

## 한도를 만났을 때 - `## 블록`이 아니다

토큰 회전 훅을 쓰는 프로젝트만 해당한다(`.dira/token-rotate.sh`가 있으면 켜져 있다). 없으면
한도도 `## 블록`이다 - 알릴 곳이 없다.

**세션이 안에서 부른 엔진**이 인증 성공 뒤 왕복만 거절당하고 리셋 시각-카운트다운이 붙으면
한도다 - 사람이 답할 것이 없다. 셋만 한다 - `bash .dira/token-rotate.sh exhausted`,
`.dira/workers/<내 워커>.sh unassign <내 해시>`(워커는 fm `owner:`), 종료(`.done`으로 안 바꾸고
**본문에 아무것도 안 적는다**). **`## 블록`을 쓰고 `unassign`하면 티켓이 답변 대기로 잠긴다 -
금지.** 가르는 질문 - **사람이 답을 쓰면 이 자리가 풀리나.** 풀리면 `## 블록`, 기다리면 되면
한도다. 판별표는 `한도.md`.

## 질문을 쓸 때 - 형식은 하나다

**`awaiting:`을 걸면서 본문에 `## 질문 n` 절을 안 쓰는 것은 금지다.** 그 절의 문항-선택지
형식은 하나뿐이고 화면은 그것만 읽어 카드를 만든다 - **쓰기 전에 `질문-형식.md`를 읽는다.**

## 회고 예산 - 페르소나 `memory/` 합 150,000 B

`CORE.md` §회고에서 메모리에 **더하는 순간** 걸린다 - 줄이 아니라 바이트고
`cat ".dira/personas/<페르소나>/memory/"*.md | wc -c`로 잰다. 넘어 있으면 **자기가 지금 만지는
그 파일 하나를 더하는 바이트보다 많이 줄이고 더한다.** 남의 파일은 안 연다. 판정은
`회고-예산.md`.

## 완료 트리거 - 아카이브 티켓을 내고 끝낸다

아카이빙을 쓰는 프로젝트만 해당한다(`.dira/personas/archive-manager/`가 있으면 켜져 있다).
자리는 **push 성공 다음 - `.done` rename 직전**이고 대상 해시는 **하나**다. 발행하는 heredoc과
`deps:`-`archives:` 판정은 `완료-트리거.md` - **끝내는 티켓이 `archive-manager`면 건너뛴다.**

## 브라우저를 띄울 때 (CDP)

띄우기 전에 `cdp.md` - 헤드리스 + 세션별 포트 + 전용 `--user-data-dir`, **사람 프로필에
헤드리스 금지**(쿠키 파괴), 볼 창은 `open -g`, 끝나면 죽인다.

## 지도

프로젝트 문서를 여기 더한다 - 스펙의 단일 출처, 코드베이스 규약, 착수 전에 읽을 것.

- `<프로젝트 스펙 문서>` - (채운다)
- `.dira/protocols/tickets.md` - 티켓 쪼개는 기준(문법은 코어 `CORE-TICKETS.md`)
- `.dira/personas/<이름>/PROFILE.md` - 각 역할의 권한-판단 기준
- `.dira/protocols/ontology.md` - 실체를 먼저 query한다(`python3 graph.py query .dira "<개념>"`)
- `.dira/protocols/epics.md` - fm에 `epic:`이 있으면 `.dira/epics/<값>/memory/` grep
