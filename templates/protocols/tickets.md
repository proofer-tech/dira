# 티켓 — 이 프로젝트가 정하는 것

티켓 문법(만드는 절차 · frontmatter · 함정 · 확인)은 **코어 `CORE-TICKETS.md`에 있다** —
세션 프롬프트의 코어 블록 머리에 찍힌 경로가 그 디렉터리다. 여기 남는 것은 아래 하나다.

## 쪼개는 기준

한 티켓 = **한 세션(5~25분) = 리뷰 가능한 하나의 변경**.

- 화면 하나, 레이어 하나, 검증 하나. "화면 3개 + 테스트"는 4개 티켓이다.
- `## Done when`이 6개를 넘으면 쪼갤 신호다.
- 쪼갠 것들 사이에 하드 선후가 있을 때만 `deps`로 엮는다. 병렬 가능한 건 병렬로 둔다.

## 에픽을 여는 자리

에픽을 여는 것은 pm이고, 여는 순간은 로드맵에 `### P###.` 블록을 세우는 그 순간이다.
그 순간 쪼개는 티켓 전부가 같은 `epic:` 값(그 P번호 본체, 예 `P273`)을 받는다 - 형식과
자리는 `epics.md`. 로드맵 항목 없이 티켓만 먼저 나면 `epic:`은 비워 둔다(정상, `(에픽
없음)`으로 모인다) - 나중에 로드맵이 서면 그때 채운다.
